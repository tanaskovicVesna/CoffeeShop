<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.gridview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".activities.EspressoActivity"
            android:label="@string/espresso" />
        <activity
            android:name=".activities.MacchiatoActivity"
            android:label="@string/macchiato" />
        <activity
            android:name=".activities.IcedCoffeeActivity"
            android:label="@string/icedCoffe" />
        <activity
            android:name=".activities.CappuccinoActivity"
            android:label="@string/cappuccino" />
        <activity
            android:name=".activities.NescafeActivity"
            android:label="@string/nescafe" />
        <activity
            android:name=".activities.TraditionalCoffeeActivity"
            android:label="@string/traditional_coffee" />
        <activity android:name=".activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>